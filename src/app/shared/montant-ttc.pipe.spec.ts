import { MontantTTCPipe } from './montant-ttc.pipe';

describe('MontantTTCPipe', () => {
  it('create an instance', () => {
    const pipe = new MontantTTCPipe();
    expect(pipe).toBeTruthy();
  });
});
